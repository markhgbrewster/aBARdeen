<%= stylesheet_link_tag 'gmaps4rails' %>
<p id="notice"><%= notice %></p>

<h1> <%= @venue.name %> </h1>

<p>
  <b>Address:</b>
  <p><%= @venue.address1 %></p>
  <p><%= @venue.address2 %></p>
  <p><%= @venue.address3 %></p>
  <p><%= @venue.address4 %></p>
  <p><%= @venue.postcode %></p>
</p>


<p>
  <b>Average Star rating:</b>
  <%= @venue.avg_star_rating %>
</p>

<p>
  <b>Health rating:</b>
  <%= @venue.health_rating %>
</p>


<p>
  <b>Health rating date:</b>
  <%= @venue.health_rating_date %>
</p>

<!--<%= gmaps4rails(@json) %>-->
<%= gmaps("markers" => {"data" => @map}, "map_options" => {"processing" => "json", "auto_adjust" => false, "auto_zoom" => false, "zoom" => 5}) %>



      <div>
      	<h2>Reviews</h2>
      </div>

      <%= button_to 'Add a new Review', new_review_path(:venue_id => @venue.id) %>
<div class= "venue_show_content">
   <% @reviews.each do |review| %>
<div class= "pinched">
   <p> <%= review.updated_at.to_formatted_s(:long) %></p>
    <p><%= review.review_summary %></p>
           <div>
              <p>Star Rating: <%= review.star_rating %></p>
              <p>Review Text: <%= review.review_text %></p>
           </div>
</div>
        <%end%>


	<p><% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      	<% end %></p>
</div>
